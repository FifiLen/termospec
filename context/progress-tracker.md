# Progress Tracker

Update this file whenever the current phase, active feature, implementation state, visual decision, or content decision changes.

## Current Phase

- Context package prepared for the Termospec website redesign.

## Current Goal

- None.

## Completed

- Removed the extra left gutter from the `/o-firmie` hero wrapper while keeping the right gutter. No tests/build run per owner request.
- Adjusted the `/o-firmie` hero lead layout so the third introductory paragraph spans the full two-column text grid on tablet/desktop. No tests/build run per owner request.
- Corrected the `/o-firmie` opening layout after the hero drifted at desktop width: the hero image is now absolutely positioned only inside its own hero wrapper instead of spanning behind the fact strip, the left hero column and fact strip have matching horizontal gutters, and the four-cell fact strip now sits as a separate band below the hero. Updated `context/content-map.md` to describe the current non-overlapping structure.
- Removed the standalone contact page and contact search-index entry. Deleted `app/pages/kontakt.vue` and `content/kontakt.md`, changed contact CTAs and footer/search quick links from `/kontakt` to `/#kontakt`, and changed Nuxt route rules so legacy `/kontakt` redirects to the homepage contact form. Updated architecture/content/project context to record that contact is an anchor section, not a separate route.
- Changed the `/o-firmie` realization gallery between team and contact from four images to three images and adjusted the desktop layout to three columns. Updated `content/o-firmie.md` and `context/content-map.md`. No tests/build run.
- Added a full-width four-photo realization gallery between `TeamGrid` and `HomeContactForm` on `/o-firmie`, reusing the first four `homeRealizationPhotos` from `data/home.ts` with minimal `gap-px` spacing. Updated `content/o-firmie.md` and `context/content-map.md`. No tests/build run.
- Adjusted the `/o-firmie` scope section per owner feedback: removed the visible explanatory paragraph above the cards and centered the `zakres działalności` label plus heading. Updated `content/o-firmie.md` so search no longer includes the removed paragraph. No tests/build run per owner request.
- Added the shared homepage `HomeContactForm` section to the bottom of `/o-firmie`, directly after `TeamGrid`, so the company page ends with the same contact/map/form experience as the homepage. Updated `content/o-firmie.md` and `context/content-map.md` so search/context include the bottom contact section.
- Renamed the company/about route from `/o-nas` to `/o-firmie` across header navigation, footer links, search quick links, Nuxt route rules, and the Nuxt Content search-index entry. Added a 301 redirect from legacy `/o-nas` to `/o-firmie` and changed `/zespol` to redirect to `/o-firmie#zespol`. Strengthened `data/about.ts` and `content/o-firmie.md` with owner-provided legacy company facts: 25 years, 500 MW total cooling capacity, mine/tunnel specialization, scientific cooperation, and international mine deliveries, while keeping the calmer editorial page layout.
- Corrected homepage realization photo carousel rendering by replacing `NuxtImg` with direct static `<img>` tags in `HomeRealizations`, so the existing `public/images/...` asset paths are linked literally instead of being transformed into broken/tiny IPX image URLs. No tests/build run per owner request.
- Reworked homepage `HomeRealizations` from text-heavy realization cards into a horizontal photo carousel using existing local product/realization assets from `public/images/products/` and `public/images/realizations/`. Added `homeRealizationPhotos` in `data/home.ts`, kept the carousel controls and product catalogue CTA, removed the visible card body/scope/number layout from the section, and updated `content/index.md` plus `context/content-map.md` to match the rendered homepage.
- Added the public `/rodo` page without running tests/build, per owner request. The RODO clause is stored in `data/rodo.ts`, rendered by `app/pages/rodo.vue`, indexed through `content/rodo.md`, linked from footer `rodo` labels, added to search quick links, and added to Nuxt route prerendering.
- Dev-server cleanup after Vite/esbuild overlay: investigated the `parsing .nuxt/tsconfig.server.json failed: Unexpected end of JSON input` overlay and confirmed `app/plugins/lenis.client.ts` was only the module that triggered parsing, not the source of the error. Stopped three stale Node/Nuxt processes running from `/Users/filiplendel/nuxt-app` on old ports so they no longer concurrently regenerate the shared `.nuxt` directory. `.nuxt/tsconfig.server.json` now parses as valid JSON, a fresh escalated dev server is running at `http://127.0.0.1:3045/`, and a local fetch confirmed `/o-nas` returns `200` with the updated hero overlap classes.
- Added the public `/dofinansowania` page for grant/funding information. The page is backed by `data/dofinansowania.ts`, renders the supplied logo groups `/grupa-dofinansowanie-grant-01.png`, `/grupa-dofinansowanie-grand-02.png`, and `/grupa-dofinansowanie-grant-03.jpg`, includes the project copy visible in the provided screenshots, links the header funding panel and footer `dotacje` item to the new route, adds `/dofinansowania` to route prerendering, and adds `content/dofinansowania.md` plus search quick-link/index updates for global search.
- Follow-up about page hero facts overlap: removed the `/o-nas` hero section top route padding, aligned the left hero column to the top with tighter top spacing, and moved the three-cell fact strip into the hero itself using a negative top margin so it overlaps the bottom of both the text and image columns instead of reading as a separate section. Updated `context/content-map.md` to describe the current hero structure. The first `npm run build` attempt failed because the previous dev server was still regenerating `.nuxt` concurrently and left a malformed generated `tsconfig.server.json`; after stopping that dev server, `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0. The known Nuxt/Icon SWR warning for `@iconify-json/lucide/icons.json` still appears during prerender. Generated HTML for `/o-nas` confirms the old `pt-24 lg:pt-28` class is absent and the new `items-start` plus `-mt-24` hero fact-strip classes are present. A fresh dev server is running at `http://127.0.0.1:3045/`, and a local fetch confirmed `/o-nas` returns `200` with the new hero classes.
- Follow-up about page hero alignment: reduced the `/o-nas` hero top and inner padding, changed the main heading to the same `text-4xl` scale as the current first homepage heading, removed the hero CTA buttons and unused hero link data, and kept the opening hero as a two-column grid where `/budynek-termospec.png` fills the full right image column with `object-cover`. Updated `content/o-nas.md` and `context/content-map.md` to match the visible page. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0; the known Nuxt/Icon SWR warning for `@iconify-json/lucide/icons.json` still appears during prerender. A dev server is running at `http://127.0.0.1:3044/`, and an approved local fetch confirmed `/o-nas` returns `200`, includes the new hero classes, and no longer includes the removed `Zobacz produkty` CTA text.
- Follow-up product header sketch fitting correction: changed the `ProductCatalogueSheet` sketch image from `max-h-full max-w-full` to explicit `h-full w-full object-contain` while keeping the removed fixed `height` prop. This preserves the natural IPX width-only image source and makes the two `1536x1024` CP(N)/CWW sketches fit inside the full header frame instead of overflowing the short wide header area. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0; the known Nuxt/Icon SWR warning for `@iconify-json/lucide/icons.json` still appears during prerender. An approved local fetch against `http://127.0.0.1:3043/produkty` returned `200`, confirmed the `h-full w-full object-contain object-bottom` class is present, and confirmed the old `height="300"` attribute is absent.
- Follow-up product header sketch fitting: adjusted `ProductCatalogueSheet` so the optional `headerSketch` no longer forces every sketch into a shared `900x300` image ratio. Header sketches now keep their natural aspect ratio and are constrained with `max-h-full max-w-full`, which prevents the CP(N) and CWW sketches from appearing enlarged or clipped in the right header column. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0; the known Nuxt/Icon SWR warning for `@iconify-json/lucide/icons.json` still appears during prerender.
- Follow-up product catalogue UI: widened the `/produkty` left catalogue index from `18.75rem` to `21rem`, changed the local product search into a lower visible input with a magnifying-glass icon and `Szukaj produktów` placeholder, removed visual active product highlighting so the current product/category text only turns burgundy, removed the deep navy KPI strip from `ProductCatalogueSheet`, and updated `/produkty` search/context notes to match the rendered catalogue. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0; the known Nuxt/Icon SWR warning for `@iconify-json/lucide/icons.json` still appears during prerender. A dev server is running at `http://127.0.0.1:3043/`, and an approved local fetch confirmed `/produkty` returns `200`, includes the new search placeholder and `21rem` left panel class, and no longer includes the removed KPI label or old active background class.
- Follow-up product catalogue naming: changed `Urządzenia chłodnicze TS` from a product category to old-site introductory context only. Updated the current product YAML records so each visible category temporarily contains one product-family record with the same title (`Urządzenia bezpośredniego działania`, `Urządzenia pośredniego działania`, `Urządzenia pośredniego działania 1-3 MW`, `Chłodnice powietrza CP(N)`, `Chłodnice wyparne CWW`), updated `/produkty` SEO/search-index copy, refreshed `content/home.yml`, and documented the temporary naming decision in `context/content-map.md`. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0, and generated payload checks confirmed the old label is no longer emitted by `/produkty`.
- Follow-up product realizations spacing: removed the visible `Realizacje` heading and all top/bottom section padding from the `ProductCatalogueSheet` photo grid so the realization images attach directly below the deep navy KPI strip. Updated `content/produkty.md` and `context/content-map.md` to describe the unheaded, flush photo grid. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0; the existing Nuxt/Icon SWR warning for missing `@iconify-json/lucide/icons.json` still appears during prerender. An approved local fetch against `http://127.0.0.1:3041/produkty` returned `200`, kept `aria-label="Realizacje"`, and confirmed the removed heading/padding classes are absent.
- Follow-up product realizations layout: removed the separate two side-by-side product-photo media band from `ProductCatalogueSheet`, replaced the previous related-realizations carousel/text cards with a full-width `Realizacje` photo grid, and temporarily repeat the available `heroImage`/`gallery` media to six visible cells until final realization photos are supplied. Updated `content/produkty.md` and `context/content-map.md` so search/context no longer describe the removed media band or carousel. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` completed with exit code 0; the build still logs the existing Nuxt/Icon SWR error for missing `@iconify-json/lucide/icons.json` during prerender. A dev server is running at `http://127.0.0.1:3041/`, and an approved local fetch confirmed `/produkty` returns `200`, includes `aria-label="Realizacje"`, and no longer includes `Powiązane realizacje` or `aria-label="Zdjęcia produktu"`.
- Follow-up product navigation: added a desktop product mega menu to `SiteHeader` using Nuxt UI `UNavigationMenu` for hover/focus behavior, backed the menu with the existing `content/products/*.yml` product families from `app.vue`, simplified the custom `ProductMegaMenu` panel into a fixed 3-column plain white card grid with no internal divider lines where each product-family card shows only the product name and its `headerSketch`, changed menu sketches from `NuxtImg` to direct static `<img>` paths, moved the desktop dropdown positioning from viewport-centered `fixed` placement to an absolute viewport under the `produkty` trigger, changed active nav styling to text underline instead of bottom-border pseudo-elements, changed product-card hover treatment to a red `brand` border without background fill, kept mobile as a direct `/produkty` link, and updated `content/index.md`, `context/ui-context.md`, and `context/content-map.md`. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes. A production server at `http://127.0.0.1:3040/` returned `200` for `/`, `/produkty`, `/o-nas`, and `/kontakt`, and direct requests to all five `/images/products/*-schemat-liniowy.png` menu sketch files returned `200 image/png`. Full hover screenshot verification was not completed because Playwright is not installed locally.
- Follow-up about page hero image framing: changed the `/o-nas` hero to a two-column opening grid where the building photo fills the full right half and removed the bottom navy caption strip from the image.
- Follow-up about page hero cleanup: removed the visible `O firmie` eyebrow badge and decorative background grid from the `/o-nas` hero, reduced the H1 scale to match the page heading rhythm, and restored the right-side building image to full color.
- Follow-up about page team section heading: added the visible `Poznaj nasz zespół` heading above the inset `TeamGrid` photo-card grid and mirrored the copy in `content/o-nas.md`.
- Follow-up about page team section framing: changed `TeamGrid` into a deep navy full-width section with standard `/o-nas` gutters, vertical spacing, and an inset `max-w-[96rem]` photo-card grid.
- Follow-up about page team card sizing: reduced `TeamGrid` card scale so more people fit in one viewport by increasing desktop grid density, lowering the card minimum height, and tightening the name/role overlay typography.
- Follow-up about page team section: simplified `TeamGrid` into a full-width photo-card grid that shows only each person's image, name, and role on the card; removed visible department grouping, counters, bios, and email links from the section UI, and updated the `/o-nas` search-index copy plus `content-map.md`.
- Follow-up about page cleanup: removed the final dark contact CTA section from `/o-nas`, deleted the unused about-page CTA data, and updated the about-page search-index copy plus `content-map.md` so the page now ends with the team section.
- Implemented `context/feature-specs/07-performance-optimization.md`:
  - ran `npx nuxt analyze --no-open` with Node `v22.20.0` and compared the Studio-enabled build against `NUXT_STUDIO_ENABLED=0`,
  - changed Nuxt Studio loading so Studio is explicitly opt-in through `NUXT_STUDIO_ENABLED=1` instead of auto-enabling in production when Studio auth variables exist,
  - added Nuxt `routeRules` prerendering for `/`, `/produkty`, `/o-nas`, and `/kontakt`,
  - replaced raw `<img>` usage with `NuxtImg` where safe for local/static assets, including product catalogue media, related realization images, homepage realization images, footer background media, and legacy product/hero components,
  - kept external Unsplash team thumbnails as native lazy `<img>` elements because IPX prerendering those remote URLs fails without network access,
  - documented the Studio and prerender decisions in `context/architecture-context.md`.
- Created project-specific context file structure based on the previous AI workflow pattern.
- Rebuilt `/produkty` away from the previous landing/documentation direction into a full-width catalogue workspace that starts directly under the fixed header: removed the visible intro/statistics surface, removed outer `max-w`/`mx-auto`/route-level side padding wrappers, kept the sticky technical index and full product-family sheets, removed the final contact CTA block from the catalogue flow, and updated the search-index/context notes to match the current no-hero/no-counter catalogue state.
- Defined Termospec-specific project overview.
- Defined visual direction using the requested colors:
  - `#9A0B2E`
  - `#120B38`
  - `#FFFFFF`
- Defined DM Sans as the main font direction.
- Defined token-based Tailwind v4 styling rules.
- Defined initial Nuxt/Vue architecture assumptions.
- Defined initial site structure and content map.
- Added open questions for unverified company/contact/offer details.
- Rebuilt `/produkty` as a utility-first one-page product catalogue:
  - removed the hidden `ProductPageHero` route intro from the visible catalogue flow,
  - kept Nuxt Content product families as the source of truth,
  - replaced the previous landing-style catalogue approach with a Nuxt UI documentation/page rhythm using `UPage`, `UPageAside`, `UPageBody`, `UPageGrid`, `UPageCard`, `UInput`, `UBadge`, and `UButton`,
  - removed the visible page-level catalogue header/intro and kept the route focused on side navigation, search, category controls, product counts, and quick product navigation,
  - changed product families into calmer full-width catalogue sheets with media, specifications, descriptions, a larger realization/application context area, features, and components,
  - removed product-level purchase/contact CTAs from the catalogue surface, product YAML, and product content schema,
  - replaced raw zinc catalogue styling with existing Tailwind v4 tokens such as `ink`, `brand`, `surface`, and `line`,
  - kept product-specific realization data as an open content question instead of inventing case studies.
- Documented owner style direction for company/about pages: avoid oversized split claim sections and follow a calmer editorial industrial layout.
- Implemented a `SiteHeader` component with GSAP scroll behavior: the sticky navigation translates out while the user scrolls and returns shortly after scrolling stops.
- Adjusted `SiteHeader` scroll behavior so the navigation appears only when scrolling up or when the page is at the top; it remains hidden after downward scrolling.
- Implemented navbar background/height behavior from `context/feature-specs/02-navbar-background.md`: the header wrapper no longer reserves layout height, the top state is transparent, and the scrolled state adds a blurred contrast surface.
- Removed the oversized `/o-nas` "Skala działania / Pozycja lidera budowana w trudnych warunkach" section and its page-specific `about-market` CSS.
- Implemented `context/feature-specs/03-smoothscroll.md`: added Lenis as a dependency, created a client-only Nuxt plugin for restrained wheel smooth scrolling, anchor offset handling, route resize handling, and reduced-motion opt-out.
- Removed native global `html { scroll-behavior: smooth; }` so Lenis is the single smooth-scroll controller.
- Implemented the product-family catalogue template for `/produkty` and `/produkty/[slug]`:
  - expanded the Nuxt Content product schema for editable product families,
  - replaced placeholder product data with Termospec cooling-device families,
  - rebuilt the product listing as an editorial grouped catalogue,
  - rebuilt the product detail page with technical overview, specs, content sections, media fallback, and product CTA,
  - documented the product YAML model in `context/content-map.md`.
- Updated the global typography system so headings use Manrope from Google Fonts while UI/body text remains DM Sans.
- Reworked product UI toward documentation-first presentation after owner feedback:
  - removed the large `/produkty` hero and catalogue stats panel,
  - changed `/produkty` into a compact technical index with category navigation and product-family rows,
  - removed artificial product placeholder graphics when final product photos are missing,
  - reduced product detail page hero scale and changed the contact block from a marketing band to a restrained technical contact section.
- Simplified `/produkty` after owner feedback:
  - removed category group headings and category navigation from the product listing,
  - removed decorative/separator lines from product-family rows,
  - made product rows use the full listing width,
  - removed product-card links and deleted the `/produkty/[slug]` route because product subpages are not planned.
- Implemented `context/feature-specs/04-product.md` for `/produkty`:
  - replaced the previous static product-card listing with a full-width blueprint/documentation catalogue,
  - preserved the existing `/produkty` route and Nuxt Content product family source,
  - added a sticky technical tree sidebar with accessible search and category/product navigation,
  - added dynamic client-side filtering by product name, category, specifications, applications, components, features, and content sections,
  - added in-page product anchors with smooth scrolling and active state tracking because product detail routes are not present in the current repo,
  - grouped product blocks by existing categories and rendered specs, applications, components, features, content sections, images, missing-photo hatching, and product CTA directly in the catalogue,
  - used flat blueprint styling with construction lines, restrained corners, no product-page shadows, and yellow hatched structural gutters.
- Removed the `/produkty` catalogue count/register bar showing product, category, and parameter totals after owner feedback.
- Removed the vertical product index strip (`TS-01`, etc.) from the left side of each `/produkty` product block after owner feedback.
- Removed the right-side media/contact column from each `/produkty` product block after owner feedback, including missing-photo placeholders, product CTA links, and the related hatched panel styling.
- Removed the product metadata line above each `/produkty` product title after owner feedback, including the category/status label such as `Urządzenia chłodnicze TS / draft`.
- Removed the right-side product count cell from each `/produkty` category/group header after owner feedback.
- Removed active-state gradients and red side markers from the `/produkty` sidebar navigation after owner feedback; sidebar hover/active states now change text color only.
- Added a compact technical intro/hero above the `/produkty` catalogue for `Urządzenia chłodnicze TS`, using owner-provided copy about underground mining applications, explosion-hazard classifications, M2 category, HFC refrigerants, and TS deployments in Polish, Czech, and Chinese mines.
- Corrected the `/produkty` yellow hatch treatment after owner feedback: removed the mistakenly added side rail implementation and kept hatching only as the visible layer in existing layout padding/gaps between white catalogue panels.
- Refactored the `/produkty` catalogue layout into a strict minimalist architectural grid mesh after owner feedback:
  - removed layout gaps, side padding, yellow hatching, full card-like borders, and decorative active backgrounds,
  - aligned the intro, sidebar, and content column to a shared two-column coordinate system,
  - replaced wrapper borders with directional one-pixel construction lines,
  - kept spacing internal through padding inside mesh cells.
- Reworked the `/produkty` mesh again using the gap-px background technique after owner feedback:
  - intro now uses a 12-column grid with a 4-column title cell and 8-column description cell,
  - sidebar/content, product parameter grids, product list grids, and notes use zinc-gray parent backgrounds with `gap: 1px`,
  - product cells are white, square, shadowless, and do not rely on individual borders for the structural lines.

## In Progress

- None.

## Next Up

1. Add context files to the repo, preferably under `/context` plus root `AGENTS.md`.
2. Confirm or update the actual project stack based on the repository.
3. Review and complete any remaining global Tailwind v4 token alignment beyond the current font and color setup.
4. Extend navigation with solution groups after solution routes and source content are confirmed.
5. Extract old/current website content into structured JSON using `content-map.md`.
6. Build the homepage from structured sections.
7. Build solution listing and detail page patterns.

## Open Questions

- Which contact data is final for publication?
- Should the new site prioritize mining/tunnel solutions or heat-pump/HVAC services?
- Are heat pumps still part of the main Termospec offer?
- Is there a final logo / brand asset package?
- Do we have real product photos, industrial photos, catalogues, certificates, or realizations?
- Final clean product photos and product-specific realization materials are still needed for the product catalogue; current product entries use one supplied local product image and temporary marked reference images until final Termospec materials are available.
- Product-family technical copy is marked `status: draft` and should be verified with client/source materials before publication.
- Should content be managed only through local files/Nuxt Content or a headless CMS?
- Should the site be Polish-only or multilingual?
- Should the contact form send email, save leads somewhere, or remain a simple mail/contact CTA?
- Should footer labels such as career, service, certificates, grants, and RODO receive dedicated standalone routes, or should they remain mapped to existing pages/sections until the content is confirmed?
- What controls should the future accessibility panel include, and should the floating accessibility icon open a custom panel or an external accessibility widget?
- Which verified realizations, photos, or project references should be assigned to each product family in `/produkty`?
- Should the final `/produkty` realization grid contain 9 photos in a strict 3x3 layout, or remain a 6-photo temporary grid arranged across three columns?
- What is the final product/category hierarchy after client confirmation, and should the current one-category/one-product placeholder records be split into real individual product names?

## Architecture Decisions

- Use context files as the source of truth for AI/code assistant work.
- Keep content separate from visual components.
- Use Tailwind v4 tokens mapped from CSS variables.
- Treat public company data as unverified until confirmed by client.
- Avoid adding backend/auth/database features until explicitly required.

## Session Notes

- The context files were prepared as a guardrail so future AI tasks do not repeatedly redesign, rename, or restructure unrelated parts of the site.
- The initial design direction follows a technical industrial B2B website, not a generic SaaS landing page.
- The requested colors and DM Sans typography are now documented in `ui-context.md`.
- Headings now use Manrope loaded from Google Fonts; DM Sans remains the UI/body font.
- Owner feedback on `/o-nas`: the "Pozycja lidera budowana w trudnych warunkach" section did not match the owner's page design style and has been removed. Future revisions should learn from the manually designed surrounding sections and avoid returning to that oversized split-headline pattern.
- Owner feedback on products: product pages should follow a documentation-first technical catalogue style, not a large landing-page hero or presentation-first layout. Avoid fake visuals, catalogue stat panels, oversized headings, and artificial copy around technical product families.
- Navbar scroll behavior was implemented with GSAP transform animation and cleanup on component unmount. `npm run build` passes; local routes `/`, `/produkty`, and `/o-nas` returned `200` from the dev server. Browser animation verification was not completed because Playwright is not installed locally and the network-enabled package-context retry was not approved.
- Navbar background/height feature from `02-navbar-background.md` was implemented. `NUXT_IGNORE_LOCK=1 npm run build` passes. Live browser verification was not completed because the in-app Browser surface was unavailable, Playwright is not installed locally, the existing Nuxt dev-server lock pointed at an unreachable `http://[::1]:3001`, and new localhost dev-server binds failed on tested ports.
- Smooth scroll feature from `03-smoothscroll.md` was implemented with Lenis settings tuned to be subtle rather than heavy (`lerp: 0.105`, `wheelMultiplier: 0.92`, no touch smoothing). `NUXT_IGNORE_LOCK=1 npm run build` passes. Live browser verification was blocked because the existing Nuxt dev-server lock pointed at unreachable `http://[::1]:3001`, and new dev-server binds failed on `127.0.0.1:3002`, `localhost:3003`, and `0.0.0.0:3004`.
- Product catalogue redesign from `04-product.md` was implemented. `NUXT_IGNORE_LOCK=1 npm run build` passes. A dev server was started at `http://127.0.0.1:3005/` with approval after sandboxed port binding failed; an approved local fetch confirmed `/produkty` returns `200` and includes the redesigned catalogue markup, search UI, and product content. Full screenshot/browser interaction verification was not completed because the in-app Browser surface was unavailable and Playwright is not installed locally.
- Follow-up product catalogue cleanup: removed the count/register bar from `/produkty`; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue cleanup: removed the vertical product index strip from product rows; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue cleanup: removed the right-side media/contact column from product rows; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue cleanup: removed the product metadata line above product titles; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue cleanup: removed the right-side product count cell from category/group headers; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue cleanup: removed active-state gradients and red side markers from the sidebar navigation; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue content: added the owner-provided `Urządzenia chłodnicze TS` intro above the catalogue and documented it in `content-map.md`; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue layout: corrected yellow hatch usage so it appears in the actual layout padding/gaps rather than as separate side rails; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue layout: refactored `/produkty` into a gapless architectural grid mesh with shared directional borders and no hatch/decorative gutters; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue layout: replaced the directional-border mesh with the gap-px background trick for clean Tailwind UI-style scaffolding lines; `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up navbar behavior: changed `SiteHeader` so it no longer returns automatically after scroll stops; it now stays hidden while scrolling down and reappears only on upward scroll/top-of-page.
- Follow-up footer layout: reduced `SiteFooter` vertical height by tightening blueprint rails, section padding, form spacing, copyright spacing, and the large bottom wordmark while preserving the current footer structure. `npm run build` passes.
- Follow-up layout cleanup: removed the yellow full-viewport gap above `SiteFooter` by deleting the page wrapper `mb-[100svh]` and replacing the root yellow background with the neutral surface token.
- Follow-up product catalogue behavior: corrected `/produkty` sidebar sticky behavior so the aside remains the sticky container, the search bar stays pinned inside its scroll area, and the sticky offset accounts for the fixed `SiteHeader` height.
- Follow-up navbar styling: added a persistent bottom border to the main navigation surface using the existing `line` color token.
- Follow-up product catalogue media: added a full-width media area to each `/produkty` product block with thumbnail gallery switching based on existing `heroImage` and `gallery` product fields, plus a neutral empty state while final product media is missing.
- Follow-up product media content: assigned `public/produkt-01.png` as the `heroImage` for the first product family, `chlodziarki-bezposredniego-dzialania`.
- Follow-up product catalogue data hygiene: replaced pasted inline demo product records and remote placeholder media in `/produkty` with the existing Nuxt Content `products` collection so the page renders verified local product-family data again. `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product catalogue gallery: moved product thumbnails above the main media stage on `/produkty`, limited the visible gallery selector to four thumbnails, and changed main/thumbnail product images to full-width contained scaling so equipment photos are not cropped. `npm run build` passes.
- Follow-up product catalogue gallery sizing: reduced the height of the main product media stage and made the top thumbnail strip smaller and right-aligned above the main image while keeping mobile thumbnails full-width for tap targets. `npm run build` passes.
- Follow-up product catalogue media placeholders: added temporary Unsplash industrial reference images to all `content/products/*.yml` product families so the `/produkty` gallery layout can be evaluated visually before final Termospec product photography is available. Each placeholder is marked with TODO comments and reference alt text. `npm run build` passes.
- Follow-up product catalogue media orientation: forced all temporary Unsplash placeholders to request 1600x900 horizontal crops and changed gallery thumbnails on `/produkty` to 16:9 with matching image dimensions. `npm run build` passes.
- Follow-up product catalogue gallery controls: removed the gray scaffold background from the media strip, made the gallery toolbar transparent, and added previous/next arrow buttons around the thumbnails with cyclic per-product image switching. `npm run build` passes.
- Follow-up product catalogue hero fix: moved `ProductPageHero` out of `app/pages/produkty` and into `app/components` so Nuxt registers it as a component instead of treating it as a product subroute.
- Follow-up product catalogue hero media: corrected the `ProductPageHero` default image path from `/images/ts-hero-bg.webp` to the actual public asset URL `/ts-hero-bg.webp`.
- Follow-up product catalogue hero alignment: matched the `ProductPageHero` horizontal gutter and technical frame inset to the `SiteHeader` navigation axis (`px-6`, `lg:px-10`) and removed the hero-only max-width wrapper.
- Follow-up product catalogue hero composition: moved the desktop right-side hero image slightly left by reducing its negative right offset.
- Follow-up product catalogue hero spacing: removed the duplicate top padding from the `/produkty` page grid wrapper so `ProductPageHero` controls its own header-aware spacing.
- Follow-up product catalogue hero cards: reduced the card corner radius to a very subtle `0.375rem` for a sharper technical look.
- Follow-up product catalogue hero card icons: corrected the icon treatment by restoring SVG icons to `h-11 w-11`, enlarging the parent circular holders to `7rem`, and reducing the inset ring so the parent circles carry the stronger visual weight.
- Follow-up error fix: restored `app/pages/produkty/index.vue` as a real product catalogue route after it had been replaced by hero-component code requiring missing props; the page again loads product families from Nuxt Content, renders `ProductPageHero`, search, grouped product sections, specifications, media controls, and CTAs.
- Follow-up runtime fix: added the missing `isScrolled` state and scroll listener cleanup to `SiteHeader` to remove the Vue runtime warning.
- Follow-up route fix: added a minimal `/kontakt` page so existing product CTA links resolve without Vue Router warnings while final contact data and form handling remain open questions.
- Follow-up TypeScript fix: made `ProductPageHero.getIcon()` return an explicit `"shield"` fallback so strict indexed array access cannot produce `undefined`. `npm run build` passes.
- Follow-up product hero TypeScript fix: typed `/produkty` hero feature presets and computed feature rows explicitly so `icon`, `kicker`, and `metric` are always present in the template. `npm run build` passes.
- Follow-up product hero TypeScript hardening: widened `/produkty` feature preset literals to `readonly FeaturePreset[]` and added a typed `toFeature()` mapper so the Vue TS plugin no longer infers a union without `icon`, `kicker`, or `metric`. `npm run build` passes.
- Follow-up `/produkty` runtime fix: replaced the accidental hero-component code in `app/pages/produkty/index.vue` with a real catalogue route that defines its own `catalogueIntro` and passes it to `ProductPageHero`, removing the `Cannot read properties of undefined (reading 'eyebrow')` crash. `npm run build` passes; local `/produkty` returned `200`.
- Follow-up homepage cleanup: temporarily cleared `/` so the home route renders no page sections and only the global `SiteHeader` and `SiteFooter` remain visible.
- Follow-up footer copy cleanup: removed the visible `//` prefixes from footer labels and the bottom coordinate line.
- Follow-up footer logo cleanup: changed the footer logo source to the existing `/termospec-logo.png` asset and rendered it as a white mark on the dark footer surface.
- Follow-up footer cleanup: reduced the footer logo size and removed the technical status/copyright/coordinate rows from the bottom of the footer.
- Follow-up homepage hero: added a simple image-led homepage hero using the newly provided `/głowna-01.png` asset, with restrained copy and links to contact/products.
- Follow-up homepage hero layout: reduced and centered the main hero heading, added three owner-provided offer cards below it, and matched their technical card styling to the `/produkty` hero card treatment. `NUXT_IGNORE_LOCK=1 npm run build` passes; local `/` returned `200` on the existing dev server and included the new card copy.
- Follow-up homepage hero card correction: rebuilt the three homepage offer cards to closely match the `/produkty` `ProductPageHero` card pattern, including the large circular icon holder, red compact title, technical number, corner details, spacing, hover lift, and blueprint bottom fade. `NUXT_IGNORE_LOCK=1 npm run build` passes; local `/` returned `200` and included the updated card markup.
- Follow-up homepage structure: added root `data/home.ts`, rebuilt `/` as a thin composition of `HomeHero`, `HomePartners`, `HomeScope`, `HomeProcess`, `HomeRealizations`, and `HomeContactForm`, and corrected homepage section imports to use the root data alias. `NUXT_IGNORE_LOCK=1 npm run build` passes; local `/` returned `200` and included the hero, partners, scope, process, realizations, and contact form text.
- Follow-up homepage partners: expanded `homePartners` from six to twelve presentation placeholders so the `HomePartners` desktop grid renders two complete rows; placeholder partner labels still require verified real names/logos before publication. `npm run build` passes; local `/` returned `200` and included the new partner placeholders.
- Follow-up global page UI: added `FloatingFundingPanel` as a fixed left-edge page panel using `/panel-dofinansowanie.png`, with a reduced height, higher viewport position, light panel background, and accessible alt text.
- Follow-up contact cleanup: changed the visible header offer CTA from a private `mailto:` link to the `/kontakt` route and removed stale placeholder contact details from the unused `HomeContactTopic` component.
- Follow-up homepage scope background: split only the `HomeScope` section background on desktop so the right half is filled by a decorative Unsplash industrial image while preserving the current heading, lead, and three-card section layout. `NUXT_IGNORE_LOCK=1 npm run build` passes while the dev server is running; local `/` returned `200` and includes the Unsplash background URL with the original three-column card grid still present.
- Follow-up homepage scope copy: updated `homeScopeItems` in `data/home.ts` to use the owner-provided card titles and descriptions for `Klimatyzacja`, `Wentylacja`, and `Energia odnawialna`; `HomeScope` now preserves explicit line breaks in card lead text.
- Follow-up homepage realizations carousel: widened `HomeRealizations` cards, removed the right-side carousel padding so the track runs to the viewport edge, and added subtle previous/next arrow controls above the carousel. `NUXT_IGNORE_LOCK=1 npm run build` passes; local `/` returned `200` and includes the new carousel controls.
- Follow-up homepage realizations media: added temporary Unsplash image arrays to each `homeRealizations` item in `data/home.ts` so the homepage realization cards render photographic grids while final Termospec realization photos are pending. `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up homepage realizations assets: renamed the newly supplied `ChatGPT...` public PNG files into `public/images/realizations/*.png` and replaced the Unsplash realization image arrays in `data/home.ts` with local asset paths. `NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up global page UI: removed the floating EU funding panel from the global app shell and deleted the unused `FloatingFundingPanel` component.
- Follow-up footer layout: aligned `SiteFooter` horizontal gutters with the homepage section axis by adding the shared `px-6 lg:px-10` wrapper spacing and `max-w-[96rem]` content width, then removed the extra edge-column horizontal padding so the footer content starts on the same axis as homepage section content. `npm run build` passes.
- Follow-up footer layout: adjusted `SiteFooter` to match the navigation-style full-width axis by removing the internal `max-w-[96rem]` wrappers and using small edge padding (`px-4 md:px-6 lg:px-10`) without changing footer content.
- Follow-up navigation search: added `@nuxt/ui` as a direct dependency, registered it before `@nuxt/content`, imported Nuxt UI CSS, added the documented Tailwind `@source` content scan, wrapped the app in `UApp`, added `LazyUContentSearch` fed by `queryCollectionNavigation("pages")` and `queryCollectionSearchSections("pages")`, added `UContentSearchButton` controls to the navigation as a full search bar on wide desktop and a compact icon button on smaller widths, and created root markdown search-index entries for the existing public routes `/`, `/produkty`, `/o-nas`, and `/kontakt`. `NUXT_IGNORE_LOCK=1 npm run build` passes while the dev server is running at `http://127.0.0.1:3000/`; local fetch checks confirmed `/`, `/produkty`, `/o-nas`, `/kontakt`, and `/__nuxt_content/pages/sql_dump.txt` return `200`, the pages dump contains `/produkty` plus product anchors, and `/zespol` is not indexed because it is only a redirect.
- Follow-up navigation search: added `data/search.ts` supplemental quick links for visible footer/UI labels that are not backed by standalone content pages, so search terms such as `kariera`, `serwis`, `o firmie`, `zakres`, and `realizacje` resolve to existing routes or anchors. Updated obvious footer dead links for career, service, company/team, and realizations to point at current valid destinations instead of unimplemented routes. Verified with local Fuse matching for `kariera`, `serwis`, `realizacje`, and `o firmie`.
- Follow-up footer route hygiene: changed remaining unimplemented footer/legal destinations (`certyfikaty`, `dotacje`, `rodo`, `polityka prywatności`, `mapa strony`) from `NuxtLink` elements into inactive labels so the current site no longer emits Vue Router warnings for routes that have not been confirmed or implemented. `NUXT_IGNORE_LOCK=1 npm run build` passes when run with Node `v22.20.0`; default Node `v20.9.0` fails before app compilation because current Nuxt CLI dependencies require newer Node APIs. A dev server is running at `http://127.0.0.1:3010/`, and local fetch checks returned `200` for `/`, `/produkty`, `/o-nas`, and `/kontakt`.
- Follow-up global search content: expanded root `content/*.md` search-index entries so global search covers the current rendered homepage sections, product catalogue intro, all current product-family YAML content, about-page copy, team YAML records, contact placeholder copy, global footer/contact/company labels, and inactive legal/footer labels. Updated `UContentSearch` Fuse options to ignore Polish diacritics so unaccented queries can match accented Polish content.
- Follow-up dev server stability: changed Nuxt Studio loading so regular `npm run dev` no longer enables Studio/Content preview/component metadata generation by default, even when Studio auth variables are present in `.env`; added `npm run dev:studio` for explicit local Studio work and pre-bundled `@vue/devtools-core`, `@vue/devtools-kit`, and `lenis` in Vite. Verified `npm run dev -- --port 3012` starts without `nuxt-studio`, without `nuxt-component-meta`, and without `.nuxt/component-meta.mjs`/preview artifacts. `NUXT_IGNORE_LOCK=1 npm run build` passes with Node `v22.20.0`.
- Follow-up navigation search: moved the global search trigger out of `SiteHeader` into a fixed `FloatingHeaderActions` control group positioned in the upper-right corner below the navigation, and added a matching inactive accessibility icon placeholder for a future accessibility panel. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH npm run build` passes; the existing dev server at `http://127.0.0.1:3010/` returned `200` and included the new floating actions markup.
- Follow-up product catalogue visibility: temporarily hid `ProductPageHero` on `/produkty`, kept the intro data in the route for easy restoration, added top offset so the catalogue starts below the fixed navigation, and updated the `/produkty` search-index entry plus `content-map.md` to reflect that the visible page now starts directly with the technical catalogue. `NUXT_IGNORE_LOCK=1 PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH npm run build` passes while the dev server is running. A dev server is running at `http://127.0.0.1:3013/`; local fetch confirmed `/produkty` returns `200`, does not include the hidden hero intro text, and still includes `Szukaj w katalogu`.
- Follow-up about page redesign: rebuilt `/o-nas` as a calmer editorial industrial page aligned with the current homepage direction, using structured `data/about.ts`, existing homepage scope/process data, and Nuxt UI primitives for badges, buttons, and icons. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes. The Nuxt dev watcher on `3014` was stopped because it repeatedly restarted Nitro after the production build touched `.nuxt`; a Node `v22.20.0` production preview server is running at `http://127.0.0.1:3015/`. Local fetch confirmed `/o-nas` returns `200` and includes the new hero, scope, and CTA copy. Full screenshot verification was not completed because Playwright is not installed locally.
- Rebuilt `/o-nas` in the current homepage style:
  - added `data/about.ts` as a structured source for about-page hero, fact strip, credibility rows, and final CTA copy,
  - replaced the old route layout with an image-led editorial page using `/budynek-termospec.png`, sharp grid cells, dark technical scope section, process grid, existing `TeamGrid`, and contact CTA,
  - reused `homeScopeItems` and `homeProcessSteps` instead of duplicating established homepage copy,
  - used Nuxt UI primitives on the page (`UBadge`, `UButton`, `UIcon`) where they fit the interface,
  - updated `content/o-nas.md` and `context/content-map.md` so global search and project context match the rendered page.
- Follow-up about page team section: simplified `TeamGrid` by removing the sticky portrait/scroll-spy interaction and replacing it with a calmer responsibility-area grid. The new section keeps the `/o-nas` mesh rhythm, groups people by department, shows compact grayscale portraits, role descriptions, available email links, and updates `content/o-nas.md` plus `context/content-map.md`. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; a dev server is running at `http://127.0.0.1:3020/`, and local fetch confirmed `/o-nas` returns `200` with the new team heading and department content while the old sticky class is absent.
- Verification note: production preview with `node .output/server/index.mjs` still logs an existing Nuxt/Icon runtime error for `@iconify-json/lucide/icons.json` on icon endpoints. The team section no longer adds its own Nuxt icon usage; the remaining issue is global icon packaging and should be handled as a separate unit before deployment verification.
- Product catalogue redesign note: `/produkty` now uses a full catalogue workspace rather than a documentation mesh or hero intro. No tests or build were run in this unit because the owner requested no testing at this stage.
- Implemented `context/feature-specs/05-katalog.md` for `/produkty`: rebuilt the route as a firm technical catalogue aligned with the current homepage visual language, added a compact catalogue introduction, replaced the Nuxt UI documentation scaffolding with native Vue catalogue markup, kept Nuxt Content `content/products/*.yml` as the product source, added shared product interfaces in `types/products.ts`, extended the content schema and YAML entries with `serviceScope` and `relatedProjects`, rebuilt `ProductCatalogueSheet` with product overview, media, parameters, applications, features, components, support scope, technical sections, related realization cards, and a calm final contact block, and updated `content/produkty.md`, `context/ui-context.md`, and `context/content-map.md`. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes. A local dev server at `http://127.0.0.1:3022/` returned `200` for `/`, `/produkty`, `/o-nas`, and `/kontakt`; `/produkty` SSR included the new intro, index, service scope, related realizations, and contact block. Full screenshot verification was not completed because Playwright, Puppeteer, and Chromium were not installed/available in the CLI environment.
- Implemented `context/feature-specs/06-product-catalogue-reset.md` adjustments for `/produkty`: kept the page as a full-viewport-width technical catalogue starting directly under the `h-16` fixed header, preserved construction lines and column separators, removed card-like nested catalogue panels from the media/spec/related-realization areas, kept route-level layout free of `max-w`, `mx-auto`, and horizontal padding, and left header, footer, homepage, global styles, product YAML data, and Nuxt Content schema untouched.
- Follow-up `/produkty` composition pass: simplified the catalogue index and the first product-family viewport, removed the separate `KARTA PRODUKTU / P/01` column, moved the product code into the product header, reduced uppercase/tracked labels, limited the first screen to the product header, one primary media area, and a compact parameter table, and moved gallery thumbnails lower in the sheet so detailed sections no longer compete with the opening product view.
- Follow-up `/produkty` ProductCatalogueSheet reset: rebuilt only the right-side product-family sheet into a simple repeatable structure with product header, two equal-height images, two structured content columns, four calm KPI blocks, and a smaller homepage-style related-realizations carousel. Removed the large `P/01` side mark, three-column applications/features/components section, dark technical-support block, numbered support grid, internal thumbnail gallery, and long stacked technical sections from the sheet. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; a dev server is running at `http://127.0.0.1:3023/`, and approved local fetch confirmed `/produkty` returns `200`, includes `Powiązane realizacje`, and no longer includes `Zakres wsparcia technicznego`, `Materiały produktu`, or `Powiązane obszary prac technicznych` in SSR output. Full visual screenshot verification was not completed because the in-app Browser surface is unavailable in this session and Playwright/Puppeteer are not installed in the project.
- Follow-up product media cleanup: renamed the three newly supplied `ChatGPT Image...` PNGs into descriptive product asset names under `public/images/products/` for the first product family: `chlodziarka-bezposredniego-dzialania-wyrobisko-ujecie-01.png`, `chlodziarka-bezposredniego-dzialania-wyrobisko-ujecie-02.png`, and `chlodziarka-bezposredniego-dzialania-schemat-liniowy.png`. Updated `content/products/chlodziarki-bezposredniego-dzialania.yml` and `content/produkty.md` so the first product uses the local product media instead of the previous temporary Unsplash gallery. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `/produkty` returns `200` and renders the two visible local media paths used by the current two-image product sheet.
- Follow-up product header sketch: added an optional `headerSketch` product media field to the Nuxt Content schema and shared `ProductFamily` type, mapped it through `/produkty`, and rendered it on the right side of the `ProductCatalogueSheet` header. The first product now uses `chlodziarka-bezposredniego-dzialania-schemat-liniowy.png` as its header sketch, while the two-image media section continues to use the product photos. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `/produkty` returns `200` and SSR includes the header sketch path plus both visible product photo paths.
- Follow-up product header layout: adjusted only `ProductCatalogueSheet` so product headers with `headerSketch` use a true two-column desktop layout. The text stays in the left column, while the sketch column now stretches as the full right half of the header instead of rendering as a small inset panel. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `/produkty` returns `200`, includes the sketch asset, and contains the new `lg:grid-cols-2` header layout class.
- Follow-up product header alignment: adjusted only the left `ProductCatalogueSheet` header column so the existing category/title/lead block is vertically centered against the right sketch column. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `/produkty` returns `200`, includes the new alignment class, and still includes the header sketch asset.
- Follow-up product KPI styling: adjusted only the `ProductCatalogueSheet` KPI row so the four product information blocks use the existing deep navy `bg-ink` surface with white text. The earlier `bg-deep` / `text-inverse` class pair was corrected because the active component system uses `ink` / `white` tokens in rendered surfaces. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `http://localhost:3000/produkty` returns `200`, includes `bg-ink` and `text-white`, and no longer includes `bg-deep`.
- Follow-up product index header styling: adjusted the left catalogue index title on `/produkty` so `Katalog produktów` sits on a deep navy `bg-ink` strip with white one-line text, while the search area remains a separate white block. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `http://localhost:3000/produkty` returns `200` and SSR includes the new `bg-ink` title strip plus `whitespace-nowrap` heading class.
- Follow-up product category register: adjusted `ProductCatalogueSheet` so the right product-family column now starts with a `h-12` deep navy category register matching the left index title strip. The register contains the product category and power range, and the main product header now starts directly with the product title and lead. Updated `content/produkty.md` and `context/content-map.md` to reflect the visible structure. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; approved local fetch confirmed `http://localhost:3000/produkty` returns `200`, includes the new `h-12`/`bg-ink` category register, and renders the current product category plus power range from source data.
- Follow-up product header separator: removed the vertical divider between the text and sketch columns in the `ProductCatalogueSheet` product header by dropping the header grid gap and using a white header wrapper.
- Follow-up product media separator: removed the vertical divider between the two large product photos in `ProductCatalogueSheet` by dropping the `gap-px bg-line` media-section scaffold.
- Follow-up product header spacing correction: restored the `ProductCatalogueSheet` category register to its fixed `h-12` height and moved the compact `py-2.5` spacing to the white product header/sketch area between the register and product photos; removed the forced desktop sketch min-height that was stretching this area.
- Follow-up product header spacing fine-tune: adjusted the white product header/sketch area from balanced `py-2.5` spacing to `pt-5 pb-0`, adding 10px more space above and removing 10px below before the product photos.
- Follow-up product header sketch spacing: adjusted only the right `ProductCatalogueSheet` sketch column so the sketch sits slightly farther below the navy category register and is anchored lower in a shorter image frame, reducing the empty space before the product photos.
- Follow-up product media spacing: removed the top padding from the two large product photo figures in `ProductCatalogueSheet`, keeping only side and bottom padding so the media no longer pushes extra space below the header area.
- Follow-up product description framing: added a top `border-line` divider above the two-column `Zastosowanie` / `Opis działania` section in `ProductCatalogueSheet` so the description blocks are visually closed after the product media.
- Follow-up product catalogue navigation: replaced the product index `IntersectionObserver` with a deterministic scroll-spy based on product sheet positions, so the left catalogue index now follows scroll, auto-opens the active category accordion, keeps the active navigation item visible inside the sticky aside, and marks the current product item more clearly. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes; local fetch confirmed `http://127.0.0.1:3024/produkty` returns `200` with the updated navigation markup.
- Follow-up product 2 media cleanup: renamed the three newly supplied product images for `chlodziarki-posredniego-dzialania` into descriptive files under `public/images/products/`: `chlodziarka-posredniego-dzialania-wyrobisko-ujecie-01.png`, `chlodziarka-posredniego-dzialania-wyrobisko-ujecie-02.png`, and `chlodziarka-posredniego-dzialania-schemat-liniowy.png`. Updated the product YAML and `/produkty` search-index entry so product 2 uses the local media instead of temporary Unsplash references. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product 3 media reuse: copied product 2 media into separate product 3 files named for `chlodziarki-posredniego-dzialania-1-3mw`: `chlodziarka-posredniego-dzialania-1-3mw-wyrobisko-ujecie-01.png`, `chlodziarka-posredniego-dzialania-1-3mw-wyrobisko-ujecie-02.png`, and `chlodziarka-posredniego-dzialania-1-3mw-schemat-liniowy.png`. Updated the product 3 YAML and `/produkty` search-index entry to use those copied local assets. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up air cooler CP(N) media cleanup: renamed the newly supplied `ChatGPT Image...` PNGs into descriptive product asset names under `public/images/products/`: `chlodnica-powietrza-cpn-wyrobisko-ujecie-01.png`, `chlodnica-powietrza-cpn-wyrobisko-ujecie-02.png`, and `chlodnica-powietrza-cpn-schemat-liniowy.png`. Updated the `chlodnice-powietrza-cpn` product YAML and `/produkty` search-index entry so the air cooler product uses local media instead of temporary Unsplash references. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up evaporative cooler CWW media cleanup: renamed the newly supplied `ChatGPT Image...` PNGs into descriptive product asset names under `public/images/products/`: `chlodnica-wyparna-cww-wyrobisko-ujecie-01.png`, `chlodnica-wyparna-cww-wyrobisko-ujecie-02.png`, and `chlodnica-wyparna-cww-schemat-liniowy.png`. Updated the `chlodnice-wyparne-cww` product YAML and `/produkty` search-index entry so the evaporative cooler product uses local media instead of temporary Unsplash references. `PATH=/Users/filiplendel/.nvm/versions/node/v22.20.0/bin:$PATH NUXT_IGNORE_LOCK=1 npm run build` passes.
- Follow-up product related-realizations cleanup: removed the visible `R/..` project-code label from related realization cards in `ProductCatalogueSheet` and updated the `/produkty` search-index copy to match the visible cards.
